doctrine:
    dbal:
        default_connection: default
        connections:
            default:
                driver: pdo_sqlite
                # memory: true
                path: "%kernel.cache_dir%/var/data.db"